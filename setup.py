# setup.py
from setuptools import setup, find_packages

# Define package metadata and contents
setup(
    name='minecraftai',           # Package name
    version='0.1',                # Version number
    packages=find_packages(),     # Automatically find all sub-packages
)
